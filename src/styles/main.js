import { StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
      position: 'relative',
      flex: 1,
      alignSelf: 'stretch',
      backgroundColor: '#fff',
      // alignItems: 'center',
    },
  });
export default styles;
